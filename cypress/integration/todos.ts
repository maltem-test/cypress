import {
  navigateTo,
  createTodos,
  toggleComplete,
  filterTodos,
  assertTodoLength,
  assertTodosStatus,
} from '../support/todos.po';

beforeEach(() => {
});

it('should add todo-list', () => {
  // To complete
});

it('should toggle complete todo-list', () => {
  // To complete
});

it('should filter todo-list', () => {

});


// TODO: Add more if you need
